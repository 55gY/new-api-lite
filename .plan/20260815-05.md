# 签到任务绑定链路构建验证计划

## 执行规则

- 本次仅验证现有渠道签到任务绑定、解除关联和构建链路，不改动业务逻辑。
- 先确认 Go 嵌入资源与前端依赖状态，再运行后端全量测试和前端生产构建。
- 每个阶段完成后更新本文件和 `plan.md` 索引；如构建受环境资源限制，保留可复现的失败摘要。

## Current State

渠道编辑页已能选择签到任务或发送显式解除关联标记；后端 `PatchChannel` 会据此更新 `checkin_task_id`。需要使用全量 Go 测试和前端生产构建验证该完整编译链路。

## Target State

后端所有 Go 包通过测试，前端生产构建完成且未报告渠道签到任务路由、表单字段或 API 调用相关的编译错误。

## Affected Files

本次为验证任务，不预期修改业务源码。若需保存验证记录，仅更新本文件和 `plan.md`。

## Execution Plan

### Phase 1：准备验证环境

- [x] Step 1.1：环境重置后的原工作区缺少 `go.mod` 与前端包清单，已创建临时验证副本并叠加当前源码；已恢复 Go 1.22、Bun 1.3.14 和前端依赖。
- [x] Verify 1：验证副本具备 `go test ./...` 与 `bun run build` 所需的完整清单和依赖。冻结锁文件与当前 Bun 版本不兼容，依赖仅在临时副本中以非冻结方式恢复，不影响原工作区。

### Phase 2：运行验证

- [x] Step 2.1：已在临时验证副本运行 `go test ./...`，全量通过；仅临时创建 `web/classic/dist/index.html` 满足入口嵌入要求，测试后已清理。
- [x] Step 2.2：已执行 `bun run build`，Vite 生产构建在 39.97 秒完成。
- [x] Verify 2：渠道编辑表单、签到任务页面、侧栏、任务服务、控制器和路由均未出现编译或打包错误。构建仅报告第三方 `lottie-web` 的 `eval` 提醒、浏览器数据过期提醒和既有大包体积提醒。

### Phase 3：归档与交付

- [x] Step 3.1：已记录测试与构建结果；原工作区在环境重置后缺少完整清单，因此使用隔离验证副本。已移除该副本及其依赖、构建产物，原工作区未写入依赖或打包文件。
- [x] Step 3.2：已更新本文件、`plan.md` 索引和交付说明。

## Rollback Plan

本次不修改业务源码。若临时创建嵌入资源或构建产物，只在验证结束后清理，不影响版本控制内容。

## Phase 完成记录

- [x] Phase 1 完成：已准备隔离的后端与前端验证环境。
- [x] Phase 2 完成：后端全量测试和前端生产构建均通过。
- [x] Phase 3 完成：验证结果已归档，临时验证环境已清理。
