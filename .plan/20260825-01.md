# 签到任务完整撤销计划

## 执行规则

- 本计划仅撤销“签到任务”功能及其后续修复所引入的调整，不触碰更早的独立功能提交。
- 每完成一个具体步骤，必须把对应 checkbox 从 `[ ]` 更新为 `[x]`。
- 每完成一个 Phase，必须重新读取 `plan.md`，并记录下一阶段内容。
- 删除功能前优先断开路由、入口和引用；本次经提交历史核实，两次专属提交可整体逆向应用，因此以 Git 反向提交确保完整性与可追溯性。
- 最终必须完成后端测试/构建、前端生产构建及残留引用检查。

## Current State

- 当前 `main` 位于 `bf4b9c6`（2026-08-17，修复定时任务无法保存）。
- 签到任务由 `99bba86`（2026-08-15，增加定时签到任务功能）引入，`bf4b9c6` 专门修复其前端保存链路。
- 两次提交共同涉及签到任务的后端模型、服务、控制器、路由、调度入口、渠道关联、前端页面/菜单/编辑弹窗、国际化、加密辅助、项目说明及签到任务例外规范。

## Target State

- 项目恢复至 `99bba86` 的父提交中不存在签到任务的状态，同时保留其后的其他独立历史变更（本次范围内不存在）。
- 不再提供 `/api/channel-checkin-task` 管理接口、签到任务调度入口、渠道签到任务关联、前端菜单/页面/编辑弹窗及相关国际化文案。
- `AGENTS.md` 不再保留签到任务的功能例外，项目继续遵循已移除异步任务的总体约束。
- 项目计划索引和实现说明不再将签到任务作为现有功能记录；本撤销计划保留为可审计的维护记录。

## Affected Files

| 范围 | 处理方式 | 依据 |
| --- | --- | --- |
| `99bba86` 的全部 27 个文件变更 | 反向应用该专属功能提交 | 提交主题为“增加定时签到任务功能” |
| `bf4b9c6` 的全部 4 个文件变更 | 先反向应用该修复提交 | 提交主题为“修复定时任务无法保存” |
| `AGENTS.md`、`plan.md`、`.plan/20260815-0*.md` 等签到任务文档 | 随专属提交反向移除，并新增本维护计划记录 | 防止功能规范与计划记录残留 |
| 本计划与 `plan.md` 索引 | 新增/更新 | 满足项目计划管理约定 |

## Execution Plan

### Phase 1：提交历史与影响范围确认

- [x] Step 1.1：确认工作区干净，当前 HEAD 为 `bf4b9c6`。
- [x] Step 1.2：检索提交历史，确认 `99bba86` 引入签到任务、`bf4b9c6` 仅修复其保存链路。
- [x] Step 1.3：核对两次提交的完整文件清单与当前签到引用。
- [x] Verify 1：确认以逆向应用这两次专属提交可完整覆盖功能实现、配置、文档与前端入口。

### Phase 2：完整撤销签到任务调整

- [ ] Step 2.1：按时间倒序反向应用 `bf4b9c6` 和 `99bba86`，保留可审计的撤销提交。
- [ ] Step 2.2：解决可能的上下文冲突；如无冲突，确认工作区仅包含撤销与本计划记录的预期变更。
- [ ] Step 2.3：更新 `plan.md` 索引，标记旧签到任务实施计划已由本计划撤销。
- [ ] Verify 2：确认签到任务相关文件、路由、调度入口、前端入口、渠道字段与规则例外均已消失。

### Phase 3：构建、测试与残留复核

- [ ] Step 3.1：运行后端聚焦测试和全量/编译验证。
- [ ] Step 3.2：运行前端生产构建。
- [ ] Step 3.3：搜索中英文签到术语、接口路径、模型/服务/控制器标识，核实仅本撤销维护记录可匹配。
- [ ] Verify 3：后端和前端构建成功，签到任务没有可访问入口或实现残留。

## Rollback Plan / Risks

- 若反向应用发生冲突，停止提交并基于冲突文件的两次原始差异逐项恢复至 `99bba86^` 的等价状态。
- 若构建揭示签到代码以外的隐藏依赖，只恢复最小必要的通用辅助逻辑；不得重新引入签到路由、调度、认证材料或用户界面。
- Git 反向提交保留完整历史；如需恢复，仅可通过反向撤销本次撤销提交完成。

## Phase 完成记录

- [x] Phase 1 完成：已确认签到功能及其修复完全限定于 `99bba86` 和 `bf4b9c6`。
- [ ] Phase 2 完成：待执行反向提交与计划索引更新。
- [ ] Phase 3 完成：待完成构建、测试与残留检查。
