# 签到任务精确移除计划

## 执行规则

- 本计划仅移除签到任务功能及其直接依赖，不通过整提交反向应用撤销共享文件中的独立优化。
- 每完成一个具体步骤，必须把对应 checkbox 从 `[ ]` 更新为 `[x]`；每完成一个阶段，必须更新本文件并复核 `plan.md`。
- 保留渠道模型重定向修复，以及渠道新增/编辑表单的非签到字段一致性修复。
- 最终必须完成后端测试/构建、前端生产构建和残留引用检查。

## Current State

- 签到任务由 `99bba86` 引入，`bf4b9c6` 补充其保存链路和关联前端文案。
- 两次历史提交混合了签到任务实现与两类独立优化：渠道模型重定向交互修复，以及渠道新增/编辑请求载荷一致性修复。
- 之前的整体反向应用已被撤销；当前基线为 `dd121fe`，其中保留原有优化和本维护计划。

## Target State

- 删除签到任务的模型、服务、控制器、路由、调度入口、渠道关联字段、前端页面/菜单/编辑弹窗、任务专用国际化文案，以及 `AGENTS.md` 的签到例外。
- 保留 `JSONEditor.jsx` 与 `EditChannelModal.jsx` 中用于模型重定向的默认键/候选模型修复。
- 保留 `EditChannelModal.jsx` 中对运行时回填字段、Azure 派生字段和新增渠道 `multi_key_mode` 包装字段的提交载荷清理。
- 保留与签到无关的文档和通用项目调整；只删除签到任务专用计划记录。

## Affected Files

| 范围 | 精确处理 | 分类依据 |
| --- | --- | --- |
| `controller/channel_checkin_task.go`、`model/channel_checkin_task.go`、`service/channel_checkin_task.go` 及其测试 | 删除 | 完全是签到任务 CRUD、执行与调度实现 |
| `main.go`、`model/main.go`、`model/channel.go`、`router/api-router.go`、`controller/channel.go` | 删除签到专属片段 | 仅新增签到调度、迁移、关联字段/校验和管理路由 |
| `common/secret_encryption.go` | 删除 | 当前仅被签到服务和测试使用 |
| `App.jsx`、`SiderBar.jsx`、`useSidebar.js`、`render.jsx`、`NotificationSettings.jsx`、`ChannelCheckinTask.jsx`、`CheckinTaskEditModal.jsx` | 删除签到页面、菜单和任务表单 | 仅为签到管理 UI 服务 |
| `EditChannelModal.jsx` | 删除签到关联和快速新增片段；保留模型映射与表单载荷修复 | `.plan/20260815-01.md` 与 `.plan/20260815-04.md` 的独立优化范围 |
| `JSONEditor.jsx` | 保留 | 完全是模型映射默认键修复 |
| `web/classic/src/i18n/locales/zh-CN.json` | 回退 `bf4b9c6` 加入的任务专用键 | 该批键服务于任务页面和编辑弹窗 |
| `AGENTS.md`、`plan.md`、`.plan/20260815-0*.md` | 删除签到例外和任务专用计划；保留 `.plan/20260815-01.md`、`.plan/20260815-04.md` 的独立优化记录 | 历史计划的目标和影响范围核对结果 |

## Execution Plan

### Phase 1：范围复核与纠偏

- [x] Step 1.1：读取 `.plan/20260815-01.md` 至 `.plan/20260815-05.md` 的历史内容。
- [x] Step 1.2：确认 `.plan/20260815-01.md` 为独立的渠道模型重定向修复。
- [x] Step 1.3：确认 `.plan/20260815-04.md` 包含独立的渠道表单请求载荷修复，签到关联部分须单独移除。
- [x] Step 1.4：撤销此前整体反向提交，恢复至 `dd121fe`，避免误删独立优化。
- [x] Verify 1：已依据逐文件历史差异确定精确删除与保留清单。

### Phase 2：恢复独立优化并精确删除签到功能

- [x] Step 2.1：已删除签到任务后端文件、路由、调度、迁移和渠道关联逻辑。
- [x] Step 2.2：已删除签到任务前端路由、侧栏入口、任务页面、编辑弹窗和渠道表单关联控件。
- [x] Step 2.3：已保留模型重定向和非签到表单载荷修复；国际化翻译键已与任务引入前版本逐键一致。既有“每日签到”用户功能文案不属于本次渠道签到任务，保持不变。
- [x] Step 2.4：已移除签到任务例外和任务专属计划记录，并更新主计划索引。
- [x] Verify 2：独立优化关键标识仍存在；渠道签到任务专属类型、路由、字段、调度、页面、弹窗和翻译键均不存在。

### Phase 3：构建、测试与残留复核

- [x] Step 3.1：已在隔离副本使用 Go 1.25.1 完成 `go test ./...` 和 `go build -trimpath`；全量测试与生产构建均通过。
- [x] Step 3.2：已在隔离副本使用 Bun 1.3.14 完成 `bun run build`；Vite 生产构建在 39.72 秒完成，仅有既有浏览器数据和大 chunk 警告。
- [x] Step 3.3：已搜索签到任务接口、类型、调度器、关联字段和界面标识；无实现、路由或菜单入口残留。保留计划中的审计说明及既有用户“每日签到”功能文案。
- [x] Verify 3：格式检查、前端构建、后端全量测试和生产构建均成功；签到任务不存在可访问入口或实现残留，模型映射与表单请求载荷优化关键标识仍存在。

## Rollback Plan / Risks

- 若精确删除导致构建错误，只恢复被签到逻辑误引用的最小通用片段，不能恢复任务路由、调度或界面。
- 渠道历史 SQLite 数据中可能保留已迁移的 `checkin_task_id` 列；不删除数据库列以避免破坏既有数据库，但代码不再读取或写入它。
- 若需要恢复签到任务，仅可在用户明确指示后，基于原始提交选择性恢复。

## Phase 完成记录

- [x] Phase 1 完成：已完成范围纠偏，下一阶段执行精确删除。
- [x] Phase 2 完成：已完成源代码与文档精确清理，下一阶段执行构建、测试和残留检查。
- [x] Phase 3 完成：已完成构建、测试和残留检查，等待提交精确撤销结果。
