# 限制配置与前端开放性审计计划

## 执行规则

本计划遵循仓库 `AGENTS.md` 的计划管理规则。本轮只读审计，不直接修改业务代码；所有候选项必须区分安全边界、部署级参数和适合管理员前端的业务限流配置。

## Current State

全局 API 限流已开放到系统设置，但项目仍包含多类环境变量、硬编码常量和后端 OptionMap 配置。尚未系统核对它们是否有对应前端入口。

## Target State

形成可复核的限制配置清单，明确每项的当前来源、前端状态、风险等级和建议处理方式，为后续分批实施提供依据。

## Affected Files

| File | Change |
| --- | --- |
| `restriction-audit-20260906.md` | 本次只读审计报告。 |
| `.plan/20260906-02.md` | 本次审计计划与完成记录。 |
| `plan.md` | 追加计划索引。 |

## Execution Plan

### Phase 1：建立审计范围

- [x] 读取 `AGENTS.md`，确认只读审计、计划记录和中文提交规则。
- [x] 确认审计目录覆盖后端配置、限流中间件、OptionMap、分层设置和经典前端设置页面。

### Phase 2：扫描限制来源

- [x] 扫描环境变量默认值和 `common/constants.go` 的限制常量。
- [x] 扫描分页、认证、验证码、缓存、连接池、请求体和中继超时等资源/安全边界。
- [x] 扫描限流中间件和调用路由，确认限制实际生效位置。

### Phase 3：核对前端入口

- [x] 对比 `model/option.go` 的 OptionMap 键与前端字段引用。
- [x] 核对速率限制、通用、性能、敏感词和令牌设置页面。
- [x] 区分已开放、仅环境变量可配、仅后端持久化和完全写死的项目。

### Phase 4：输出分类结论

- [x] 将全局 Web、关键接口、搜索、上传、下载和邮箱验证码限流列为优先开放候选。
- [x] 将敏感词停止策略、流缓存队列、模型分组限流和用户可用分组列为需产品确认的候选。
- [x] 将请求体、文件、缓冲区、连接池、TOTP 和验证码缓存列为不建议普通前端开放的安全/部署边界。
- [x] 编写 `restriction-audit-20260906.md`。

## Verify

通过源码扫描结果、OptionMap/前端字段交叉审计和实际调用点核对完成验证。本轮未修改业务代码，未执行前端构建。

## Rollback Plan / Risks

本轮只新增审计文档和计划记录，不涉及运行时行为。后续实施时应按请求限流、安全边界和部署参数分批修改，避免一次性开放所有低层参数造成误配置风险。

## Phase 完成记录

- Phase 1：2026-09-06 完成。
- Phase 2：2026-09-06 完成。
- Phase 3：2026-09-06 完成。
- Phase 4：2026-09-06 完成。
