# 限制配置与前端开放性审计报告

## 审计结论

本次审计覆盖 `common/`、`middleware/`、`setting/`、`controller/`、`router/`、`model/` 以及经典前端设置页面。项目中**仍有一组与请求频率直接相关的限制没有开放到系统设置前端**，其中包括全局 Web 限流、关键接口限流、搜索限流、上传/下载限流和邮箱验证码限流。此前刚开放的全局 API 限流已确认具备后端系统选项和前端入口，不再属于遗漏项。

同时，项目中还存在若干**资源保护和安全防护边界**，例如请求体大小、文件下载大小、分页最大页长、TOTP 失败锁定和验证码缓存上限。它们并不适合直接开放给普通管理员前端；其中资源类配置适合继续保留为部署级环境变量，认证安全类配置建议继续硬编码，避免后台误操作降低安全性。

## 分类结果

| 优先级 | 配置类别 | 当前来源 | 前端状态 | 建议 |
| --- | --- | --- | --- | --- |
| P1 | 全局 Web 限流 | 环境变量，默认 60 次/180 秒，默认启用 | 未开放 | 纳入“速率限制”设置，至少提供开关、次数和周期。 |
| P1 | 关键接口限流 | 环境变量，默认 20 次/20 分钟，默认启用 | 未开放 | 纳入“速率限制”设置，但设置最小安全下限，不建议允许任意关闭。 |
| P1 | 搜索限流 | 环境变量，默认 10 次/60 秒，默认启用 | 未开放 | 纳入“速率限制”设置，按用户维度说明。 |
| P1 | 上传/下载限流 | `common/constants.go` 硬编码 10 次/60 秒 | 未开放 | 改为环境变量或系统选项；优先系统选项，便于处理后台批量操作触发 429。 |
| P1 | 邮箱验证码限流 | `middleware/email-verification-rate-limit.go` 硬编码 2 次/30 秒 | 未开放 | 建议开放为受保护的安全配置，设置最小值和最大值，避免被误调为无保护。 |
| P2 | 模型请求限流分组覆盖 | 基础值已有前端，分组 JSON 仅有后端 OptionMap | 未开放 | 若项目继续使用分组差异化限流，应增加结构化前端编辑器；否则隐藏或删除未使用入口。 |
| P2 | 敏感词停止生成、流缓存队列长度 | 已有后端 OptionMap，但前端敏感词页面未见入口 | 未开放 | 与“屏蔽词过滤设置”合并开放，并补充范围说明。 |
| P2 | 用户可用分组 JSON | 已有后端 OptionMap，前端未见对应字段 | 未开放 | 如果管理员需要维护分组，应提供结构化编辑；不要让管理员直接编辑原始 JSON。 |
| P2 | 每用户最大 Token 数 | 分层配置 `token_setting.max_user_tokens` | 已开放 | `SettingsGeneral.jsx` 已有入口，无需重复实现。 |
| P2 | 磁盘缓存阈值和最大容量 | 分层配置 `performance_setting.*` | 已开放 | `SettingsPerformance.jsx` 已有入口，无需重复实现。 |
| P3 | 请求体、文件、流扫描缓冲区大小 | 环境变量，默认 64/128/128 MB | 未开放 | 继续作为部署级环境变量，不建议前端动态修改，避免内存和安全风险。 |
| P3 | 中继超时、连接池、数据库连接池 | 环境变量 | 未开放 | 继续作为部署级环境变量，属于运维调优而非业务设置。 |
| P3 | 分页最大页长 100、默认页长 10 | 代码硬编码 | 未开放 | 保留硬上限；默认页长可由前端组件或用户偏好控制，不必作为全局后台设置。 |
| P3 | TOTP 失败次数 5、锁定 300 秒 | 代码硬编码 | 未开放 | 继续硬编码，属于认证安全边界；如需企业合规，应增加环境变量而非普通前端开关。 |
| P3 | 验证码有效期 10 分钟、缓存清理阈值 10 | 代码硬编码 | 未开放 | 有效期可考虑环境变量；缓存清理阈值属于实现保护，不建议前端开放。 |

## 关键证据

### 1. 尚未开放的请求限流

`common/init.go:117-127` 只从环境变量初始化 `GlobalWebRateLimit*`、`CriticalRateLimit*` 和 `SearchRateLimit*`。虽然它们具有环境变量入口，但 `model/option.go` 没有对应的系统选项，前端也没有对应字段。相关默认值为：全局 Web 60 次/180 秒、关键接口 20 次/1200 秒、搜索 10 次/60 秒。

`common/constants.go:155-164` 将上传和下载限流固定为 10 次/60 秒，并且 `middleware/rate-limit.go:109-116` 直接消费这些常量。它们既没有环境变量入口，也没有前端设置，属于当前最明确的写死限制。

`middleware/email-verification-rate-limit.go:14-17` 将邮箱验证码限制固定为 2 次/30 秒，并在 Redis 和内存两条路径共同使用。该限制本身合理，但没有任何配置入口。

### 2. 已开放但容易被误判为遗漏的配置

`common/init.go:113-115` 的全局 API 限流默认值已经是关闭、180 次/180 秒；`model/option.go` 已注册 `GlobalApiRateLimitEnable`、`GlobalApiRateLimitNum` 和 `GlobalApiRateLimitDuration`；前端速率限制页面也已使用这三个字段。因此该项已完成开放，不需要重复改造。

`setting/operation_setting/token_setting.go:6-17` 的 `token_setting.max_user_tokens` 已注册为分层配置，前端 `SettingsGeneral.jsx` 已提供输入框。`setting/performance_setting/config.go` 的磁盘缓存相关字段也已经由 `SettingsPerformance.jsx` 提供入口。

### 3. 已有后端选项但没有前端入口

`setting/sensitive.go:10-14` 定义了 `StopOnSensitiveEnabled` 和 `StreamCacheQueueLength`，`model/option.go` 也负责其持久化更新，但当前设置页面只覆盖敏感词文本与检测开关，未发现这两个字段的表单入口。

`setting/rate_limit.go:15-34` 支持按用户分组覆盖模型请求限流，序列化键为 `ModelRequestRateLimitGroup`。基础模型限流已在前端开放，但该分组覆盖 JSON 没有结构化管理界面。

`setting/user_usable_group.go:26-40` 支持持久化用户可用分组，`UserUsableGroups` 也进入 OptionMap，但未发现前端字段。该项是否需要开放取决于项目是否继续允许后台维护用户分组；若开放，应使用表格编辑而不是裸 JSON 文本。

## 建议实施顺序

第一批建议只处理请求频率限制：全局 Web、关键接口、搜索、上传、下载和邮箱验证码。它们共同位于“速率限制”概念下，管理员有明确运维需求，且能够直接解释后台批量操作遇到的 429 问题。关键接口和邮箱验证码应设置不可绕过的最小安全值，并保留环境变量作为首次启动默认值。

第二批处理已有 OptionMap 但没有前端入口的业务设置：敏感词停止策略、流缓存队列和用户可用分组。模型限流分组建议单独设计结构化编辑器，不建议把原始 JSON 直接暴露给后台管理员。

第三批不建议开放到普通前端：请求体、文件、缓冲区、连接池、数据库连接、TOTP 锁定和验证码缓存等配置应继续由部署环境或代码安全边界控制。

## 审计范围与限制

本次是只读审计，没有修改业务代码，也没有改变已提交的全局 API 限流实现。前端依赖目录未安装，因此未执行前端生产构建；后端限制项通过源码扫描和调用点核对完成。
