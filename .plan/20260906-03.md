# 业务限制配置前端开放计划

## 执行规则

本计划遵循仓库 `AGENTS.md`。本次只在本地修改、测试并创建本地中文提交；除非用户明确要求“提交到远程/GitHub 仓库”，不得执行 `git push`。

## Current State

审计确认全局 Web、关键接口、搜索、上传、下载、邮箱验证码等限流，以及模型限流分组、敏感词行为、流缓存队列和用户可用分组尚未完整开放到管理员前端。

## Target State

通过系统 OptionMap 动态配置和经典前端设置页面开放上述业务限制，并对请求次数、周期、队列长度和 JSON 分组配置进行后端校验。请求体、文件大小、连接池、TOTP 锁定等部署级或认证安全边界不开放到普通前端。

## Affected Files

- `common/constants.go`
- `common/init.go`
- `controller/option.go`
- `middleware/email-verification-rate-limit.go`
- `model/option.go`
- `setting/user_usable_group.go`
- `web/classic/src/components/settings/RateLimitSetting.jsx`
- `web/classic/src/pages/Setting/RateLimit/SettingsRequestRateLimit.jsx`
- `web/classic/src/pages/Setting/Operation/SettingsSensitiveWords.jsx`
- `web/classic/src/pages/Setting/Operation/SettingsGeneral.jsx`

## Execution Plan

### Phase 1：确认架构与范围

- [x] 复用现有环境变量、OptionMap、`/api/option/` 和设置页面。
- [x] 明确不开放部署级和认证安全边界。

### Phase 2：后端实现

- [x] 将 Web、关键接口、搜索、上传、下载和邮箱验证码限制统一接入动态变量。
- [x] 将上传、下载和邮箱验证码的固定值改为环境变量默认值。
- [x] 增加 OptionMap 初始化和动态更新分支。
- [x] 增加请求次数、周期、流缓存队列和用户分组 JSON 校验。
- [x] 将用户分组更新改为校验成功后原子替换。

### Phase 3：前端实现

- [x] 在速率限制页面增加 Web、关键接口、搜索、上传、下载和邮箱验证码配置。
- [x] 增加模型限流分组 JSON 配置入口。
- [x] 在敏感词页面增加停止策略和流缓存队列长度。
- [x] 在通用设置页面增加用户可用分组配置入口。
- [x] 使用 Prettier 格式化并通过修改文件检查。

### Phase 4：验证与交付

- [x] `go test ./middleware ./model ./controller ./setting/...` 通过。
- [x] `git diff --check` 通过。
- [x] 修改 JSX 文件的 Prettier 检查通过。
- [ ] 前端完整构建：因项目基线缺少 `@douyinfe/semi-illustrations` 依赖而失败，非本次修改引入。
- [x] 未执行远程推送。

## Rollback Plan / Risks

如需回滚，可仅撤销本计划涉及的十个业务文件。Redis 限流周期上限保持为 1200 秒，与 `RateLimitKeyExpirationDuration` 一致。JSON 配置应通过后端校验后再更新内存，避免非法内容覆盖有效配置。

## Phase 完成记录

- Phase 1：2026-09-06 完成。
- Phase 2：2026-09-06 完成。
- Phase 3：2026-09-06 完成。
- Phase 4：2026-09-06 完成；未推送远程仓库。
