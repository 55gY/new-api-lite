# staticcheck P0 正确性修复与安全清理计划

## 目标

在 `2c3eafc` 全库健康审计基线后，按保守实施策略修复已证实的 P0 正确性问题，并仅删除无状态、无协议/迁移风险的强证据死代码。公开弃用路由、响应写失败语义、数据库迁移、Redis/cache、quota/TTS 与高敏感协议 DTO 保持不变。

## 实施记录

- [x] 建立干净 Git 基线并限定实施范围。
- [x] 修复 Coze 创建聊天响应：读取 body 后关闭，严格检查 JSON 解码错误和上游 `code`；新增损坏 JSON、上游错误及成功载荷测试。
- [x] 修复 `cleanNullValues`：使用原地过滤，保证连续 null 数组元素不会在 range 缩容时被跳过；新增连续/首尾 null、嵌套 map 与空数组回归测试。
- [x] 清理两条镜像 OpenAI-to-Claude 流转换路径中无消费者的 usage 回退赋值，保持等待最终 usage chunk 的既有 return 控制流。
- [x] 删除无调用方的 `fieldReplacer`、Claude 1h cache 常量和 Ali `syncModels` 残留。
- [x] 运行定向测试：`go test ./common ./relay/helper ./relay/channel/coze ./relay/channel/ali ./service ./service/relayconvert/internal/oai_chat`。
- [x] 最终隔离副本验证：前端 `bun run build`、Go `go test ./...`、`go build -trimpath ./...`、`go vet ./...`、`CGO_ENABLED=1 go test -race ./...` 全部通过。
- [x] staticcheck 物理日志行由 134 降至 127；保留响应写失败、弃用 API、迁移/cache/协议 DTO 等需兼容性确认的候选。

## 风险与回滚

本轮变化集中于响应解码、工具 schema 规范化和私有无调用符号；可通过本次单一 Git 提交完整回滚。不得基于 U1000 批量删除公开 API、数据库迁移函数、缓存状态函数或上游协议数据结构。
